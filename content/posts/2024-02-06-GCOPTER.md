---
layout: post
title: GCOPTER
date: 2024-02-06
categories: Robotics
tags:
  - self-study
  - control-planning
math: "true"
draft: "true"
---
# GCOPTER

## Review with Code Implementations

Differential flat 한 멀티콥터의 경로를 생성하기 위해 $\cal{T}_\text{MINCO}$ 로 flat-output 궤적을 변수화한다.

```cpp
if (!processCorridor(hPolytopes, vPolytopes))
{
	return false;
}

const Eigen::Matrix3Xd deltas = shortPath.rightCols(polyN) - shortPath.leftCols(polyN);
pieceIdx = (deltas.colwise().norm() / lengthPerPiece).cast<int>().transpose();
pieceIdx.array() += 1;
pieceN = pieceIdx.sum();

minco.setConditions(headPVA, tailPVA, pieceN);
```

SFC 에서 생성된 polytopes 를 $\cal{H}$-representation 에서 $\cal{V}$-representation 으로 바꿔준다.

이를 가지고 $\delta$ 와 $P_i$ 를 만들어준다.

> After assigning a fixed number of pieces into each $P_i$ , variable transformations are applied to eliminate all direct constraints. User-defined $\cal{G}_D$ are also transformed into $\cal{G}$ via $\Psi_x$ and $\Psi_u$.

![[GCOPTER_fig2.png]]
![[GCOPTER_flatness.png]]

이후에는 위와 같이 constraints 를 $\Psi_x$ and $\Psi_u$ 로 제거하는 과정이 이루어진다.

GCOPTER 에서 사용하는 `costFunctional` 을 바탕으로 단계별로 살펴보자.

```cpp
static inline double costFunctional(void *ptr, const Eigen::VectorXd &x, Eigen::VectorXd &g)
{
	GCOPTER_PolytopeSFC &obj = *(GCOPTER_PolytopeSFC *)ptr;
	const int dimTau = obj.temporalDim;
	const int dimXi = obj.spatialDim;
	const double weightT = obj.rho;
	Eigen::Map<const Eigen::VectorXd> tau(x.data(), dimTau);
	Eigen::Map<const Eigen::VectorXd> xi(x.data() + dimTau, dimXi);
	Eigen::Map<Eigen::VectorXd> gradTau(g.data(), dimTau);
	Eigen::Map<Eigen::VectorXd> gradXi(g.data() + dimTau, dimXi);

	forwardT(tau, obj.times);
	forwardP(xi, obj.vPolyIdx, obj.vPolytopes, obj.points);

	double cost;
	obj.minco.setParameters(obj.points, obj.times);
	obj.minco.getEnergy(cost);
	obj.minco.getEnergyPartialGradByCoeffs(obj.partialGradByCoeffs);
	obj.minco.getEnergyPartialGradByTimes(obj.partialGradByTimes);

	attachPenaltyFunctional(obj.times, obj.minco.getCoeffs(),
				obj.hPolyIdx, obj.hPolytopes,
				obj.smoothEps, obj.integralRes,
				obj.magnitudeBd, obj.penaltyWt, obj.flatmap,
				cost, obj.partialGradByTimes, obj.partialGradByCoeffs);

	obj.minco.propogateGrad(obj.partialGradByCoeffs, obj.partialGradByTimes,
				obj.gradByPoints, obj.gradByTimes);

	cost += weightT * obj.times.sum();
	obj.gradByTimes.array() += weightT;

	backwardGradT(tau, obj.gradByTimes, gradTau);
	backwardGradP(xi, obj.vPolyIdx, obj.vPolytopes, obj.gradByPoints, gradXi);
	normRetrictionLayer(xi, obj.vPolyIdx, obj.vPolytopes, cost, gradXi);

	return cost;
}
```

`forwardT` 에서는 $T=e^{[\tau]}$ 로 diffeomorphism 을 적용한다. 자세한 내용은 아래 이미지와 [논문](https://arxiv.org/abs/2103.00190) 의 V. **A. Temporal Constraint Elimination** 을 참고하면 된다.

![[GCOPTER_temp_elim_Prop1.png]]

```cpp
static inline void forwardT(const Eigen::VectorXd &tau,
                            Eigen::VectorXd &T)
{
    const int sizeTau = tau.size();
    T.resize(sizeTau);
    for (int i = 0; i < sizeTau; i++)
    {
        T(i) = tau(i) > 0.0
                    ? ((0.5 * tau(i) + 1.0) * tau(i) + 1.0)
                    : 1.0 / ((0.5 * tau(i) - 1.0) * tau(i) + 1.0);
    }
    return;
}
```

코드에서는 $e^{[\tau]}$ 대신 다항식으로 근사하여 이용하였다. ([[2023-12-02-FastRacing|Fast-Racing]] 에 있는 구버전 `gcopter.hpp` 에서는 조건에 따라 사용하였으나 최근 optimizer 에서는 대부분 이 구현버전으로 사용함.)

마찬가지로 `forwardP` 에서는 $\mathbf{q}(\xi)$ 로 Polyhedral spatial constraint ellimination 을 수행한다.

```cpp
static inline void forwardP(const Eigen::VectorXd &xi,
                            const Eigen::VectorXi &vIdx,
                            const PolyhedraV &vPolys,
                            Eigen::Matrix3Xd &P)
{
    const int sizeP = vIdx.size();
    P.resize(3, sizeP);
    Eigen::VectorXd q;
    for (int i = 0, j = 0, k, l; i < sizeP; i++, j += k)
    {
        l = vIdx(i);
        k = vPolys[l].cols();
        q = xi.segment(j, k).normalized().head(k - 1);
        P.col(i) = vPolys[l].rightCols(k - 1) * q.cwiseProduct(q) +
                    vPolys[l].col(0);
    }
    return;
}
```

위 코드 구현은 [논문](https://arxiv.org/abs/2103.00190) 의 수식 (85) (아래 이미지)와 같다.

![[GCOPTER_spatial_elim_eqn85.png]]

그 후에는 [논문](https://arxiv.org/abs/2103.00190) 의 수식 (18) 을 먼저 풀어내고 그에 따른 gradient 도 계산해둔다.

![[GCOPTER_eqn18.png]]

![[GCOPTER_min_without_costf.png]]

```
double cost;
obj.minco.setParameters(obj.points, obj.times);
obj.minco.getEnergy(cost);
obj.minco.getEnergyPartialGradByCoeffs(obj.partialGradByCoeffs);
obj.minco.getEnergyPartialGradByTimes(obj.partialGradByTimes);
```

이 과정이 `costFunctional` 에서 위 4줄의 코드라인이 수행하는 작업이고, 최종 목적함수인 [논문](https://arxiv.org/abs/2103.00190) 수식 (91) 에서 $\frac{\partial J}{\partial \tau}, \frac{\partial J}{\partial \xi}$ 를 하는 것과 같다.

그리고 [[2023-11-29-WholebodyMotionPlanning|Whole-body Motion planning]] 이나 ZJU-Fast Lab 에서 추가적으로 penalty term 으로 넣고자 하는 부분이 `attachPenaltyFunctional` 에 구현된다.

GCOPTER 에서는 $f, Thr, q,\Omega$ 에 대해 constraint 를 주었다. 각 penalty term 에서 cost 와 gradient 를 구하고 control 과 관련한 constraint 는 flatness 를 이용해 다시 $p,v,a,j$ 로 변환해 $\partial C,\partial T$ 를 계산한다.

> Finally, we obtain the cost function (91). Apparently, the gradient propagation is derived for all layers except $\Psi_x$ and $\Psi_u$. One can either apply Automatic Differentiation (AD) [57] to $\Psi_x$ and $\Psi_u$ or derive the gradient propagation analytically by following the reverse-mode AD.
> The efficiency is the same as the flatness map as ensured by Baur-Strassen Theorem [58]. The differentiation is only needed for the given flat dynamics once and for all.
> With available gradient, the relaxation (91) is then solved by the L-BFGS algorithm [59].

## Safe Flight Corridor

[Geometrically Constrained Trajectory Optimization](https://arxiv.org/abs/2103.00190) 논문에서 언급한 SFC 알고리즘의 이름과 해당 논문들

>[!quote] SFC Algorithms
> the Parallel Convex Cluster Inflation (PCCI) [31]
> - Gao, Fei, et al. "Teach-repeat-replan: A complete and robust system for aggressive flight in complex environments." _IEEE Transactions on Robotics_ 36.5 (2020): 1526-1545. [Arxiv](https://arxiv.org/abs/1907.00520)
> 
> **DecompUtil**, the Regional Inflation by Line Search (RILS) [38]
> - S. Liu et al., "Planning Dynamically Feasible Trajectories for Quadrotors Using Safe Flight Corridors in 3-D Complex Environments," in IEEE Robotics and Automation Letters, vol. 2, no. 3, pp. 1688-1695, July 2017, doi: 10.1109/LRA.2017.2663526.
>
> the Safe-Region RRT* Expansion [64]
> - Gao, Fei et al. “Flying on point clouds: Online trajectory generation and autonomous navigation for quadrotors in cluttered environments.” _Journal of Field Robotics_ 36 (2018): 710 - 733.
>
> Iterative Regional Inflation by Semidefinite programming (IRIS) [65]
> - Deits, R., Tedrake, R. (2015). Computing Large Convex Regions of Obstacle-Free Space Through Semidefinite Programming. In: Akin, H., Amato, N., Isler, V., van der Stappen, A. (eds) Algorithmic Foundations of Robotics XI. Springer Tracts in Advanced Robotics, vol 107. Springer, Cham. https://doi.org/10.1007/978-3-319-16595-0_7
>
> [63]
> - W. Hönig, J. A. Preiss, T. K. S. Kumar, G. S. Sukhatme and N. Ayanian, "Trajectory Planning for Quadrotor Swarms," in IEEE Transactions on Robotics, vol. 34, no. 4, pp. 856-869, Aug. 2018, doi: 10.1109/TRO.2018.2853613.

### How to run

레이싱 트랙 환경에서 GCOPTER 로 경로 생성 후, Geometric controller 로 tracking control

```
roslaunch px4_controller race_track_env.launch

roslaunch px4_controller race_track_planning.launch

roslaunch px4_controller geometric_controller.launch
```


```python
>>> rotor_radius = 0.128
>>> base_to_rotor = math.sqrt(0.13 ** 2 + 0.22 ** 2)
>>> base_to_rotor
0.25553864678361277
>>> total_radius = rotor_radius + base_to_rotor
>>> total_radius
0.38353864678361277
```

## Analyze Penalty Functions in Various Applications

각 논문이나 코드마다 `attachPenaltyFunctional` 의 구현 디테일이 차이가 있는데, 자세 고려부분에 집중해서 살펴보았다.

### GCOPTER

```cpp
L = hIdx(i);
K = hPolys[L].rows();
for (int k = 0; k < K; k++)
{
    outerNormal = hPolys[L].block<1, 3>(k, 0);
    violaPos = outerNormal.dot(pos) + hPolys[L](k, 3);
    if (smoothedL1(violaPos, smoothFactor, violaPosPena, violaPosPenaD))
    {
        gradPos += weightPos * violaPosPenaD * outerNormal;
        pena += weightPos * violaPosPena;
    }
}
```

GCOPTER 에서는 `vector<MatirxX4d> hPolys` 가 normal vector 와 distance 로 이루어져 있다. 따라서 `violaPos` 를 map 에서 만들어진 SFC 안에 있는지 확인하는 방식이다.

### [[2023-12-02-FastRacing|Fast-Racing]]

```cpp
idx = idxHs(i);
K = cfgHs[idx].cols();
for (int k = 0; k < K; k++)
{
    outerNormal = cfgHs[idx].col(k).head<3>();
    point = cfgHs[idx].col(k).tail<3>();
    beta0dOuterNormalTp = beta0 * outerNormal.transpose();
    gradSdT = gradSdTxyz.transpose() * outerNormal;
    outerNormaldVel = outerNormal.dot(vel);
    beta2dOuterNormalTp = beta2 * outerNormal.transpose();
    gradSdCx = beta2dOuterNormalTp * dxB;
    gradSdCy = beta2dOuterNormalTp * dyB;
    gradSdCz = beta2dOuterNormalTp * dzB;

    eNormGd = (rotM.transpose() * outerNormal).array() * ellipsoid.array();
    eNorm = eNormGd.norm();
    eNormGd /= eNorm;
    signedDist = outerNormal.dot(pos - point) + eNorm;
    eNormGd.array() *= ellipsoid.array();

    signedDist += safeMargin;
    if (signedDist > 0)
    {
        signedDistSqr = signedDist * signedDist;
        signedDistCub = signedDist * signedDistSqr;
        gradSdC = beta0dOuterNormalTp +
                    gradSdCx * eNormGd(0) +
                    gradSdCy * eNormGd(1) +
                    gradSdCz * eNormGd(2);
        gradSignedDt = alpha * (outerNormaldVel +
                                gradSdT(0) * eNormGd(0) +
                                gradSdT(1) * eNormGd(1) +
                                gradSdT(2) * eNormGd(2));
        gdC.block<6, 3>(i * 6, 0) += omg * step * ci(0) * 3.0 * signedDistSqr * gradSdC;
        gdT(i) += omg * ci(0) * (3.0 * signedDistSqr * gradSignedDt * step + signedDistCub / cons(i));
        pena += omg * step * ci(0) * signedDistCub;
    }
}
```

Fast-Racing 에서는 `hPolys` 와 같은 역할인 `vector<MatrixXd> cfgHS` 는 normal vector 와 point 로 이루어져 있다. 그래서 $Q=\text{diag}(r_e,r_e,h_e)$ 에 대해 ellipsoid $\epsilon(t)=\Big\{R(t)Qx+p(t)|\|x\|_2\leq 1\Big\}$ 로 드론을 모델링하고, SFC polyhedron 안에 있음을 다음과 같이 수식화한다.

$$
\begin{align}
\epsilon_i(t)&\in\cal{P}^{\cal{H}}_j\\
j&=[i/K],\ \forall t\in[0,T_i]\\
\text{is equivalent to} \\
\Big[[A_jR_i(t)Q]^2\mathbf{1}\Big]^{1/2}&+A_jp_i(t)-b_j\preceq 0 \\
j&=[i/K],\ \forall t\in[0,T_i]
\end{align}
$$

이 때 $[\cdot]^2,[\cdot]^{1/2}$ 는 entry-wise 곱이고, $\cal{P}^{\cal{H}}_j=\Big\{x\in\mathbb{R}^m|A_jx\preceq b_j\Big\}$ 이다.

위 수식을 계산하는 파트는 아래와 같다. `.array()` 로 곱해줌으로써 entry-wise 곱이 되었다

```cpp
eNormGd = (rotM.transpose() * outerNormal).array() * ellipsoid.array();
eNorm = eNormGd.norm();
eNormGd /= eNorm;
signedDist = outerNormal.dot(pos - point) + eNorm;
```

하지만 그 다음 라인에서 `ellipsoid.array()` 를 다시 곱연산해주는 이유는 normalization 된 `eNormGd` 를 다시 ellipsoid 크기로 변환해주는 것이다.
